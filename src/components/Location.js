import React from "react";

function Location({ city }) {

    //DESTRUCTURING
    // const { city } = props;

    return (
        <div>
            <h1>{city}</h1>
        </div>
    );
}

export default Location;
